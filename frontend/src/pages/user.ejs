<% layout('app', { title: 'User', activePage: 'user' }) %>

<div class="card">
  <div class="card-header">
    <h5>Thông Tin Người Dùng</h5>
  </div>
  <div class="card-body">
    <p>Tên: <%= user.name %></p>
    <p>Email: <%= user.email %></p>
    <!-- Thêm các thông tin khác nếu cần -->
  </div>
</div>
